---
description: List Rig projects and show which ones are running
---

List the user’s Rig projects and show which are running.

Use Rig MCP `list_projects` (and `get_status` if you need one project). If tools are missing or return 401, follow the setup skill — do not guess from the terminal.
